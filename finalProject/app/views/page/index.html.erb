<%= stylesheet_link_tag "page" %>
<% if current_user %>
  <div>
    <% if current_user.admin? %>
  
    <nav class= "navbar navbar-expand-sm navbar-light">
    <ul class= "navbar-nav">
      <li class = "nav-item active"><%= link_to 'Home', page_index_path, :class => "nav-link"%></li>
      <li class= "nav-item"><%= link_to 'Generate Reports', page_adminReport_path, :class => "nav-link" %></li>
      <li class= "nav-item"><%= link_to 'Major Requirements', page_majorRequirements_path, :class => "nav-link" %></li>
      <li class= "nav-item"><%= link_to 'Add New Course', page_addCourse_path, :class => "nav-link" %></li>
      <li class= "nav-item"><%=link_to "Sign Out", destroy_user_session_path, method: :delete, :class => "nav-link"%></li>
    </ul>
    </nav>
    <h2 class="text-center">Welcome</h2>
 

    <% else %>
      <% if current_user.sign_in_count == 1 && !current_user.admin %>
        <% check = false %>
        <% @DegreeSearch.each do |degree| %>
          <% if degree.user_id == current_user.id %>
           <% check = true %>
            <%end%>
        <%end%>
        <% if !check %>
          <% DegreeInProgress.create(user_id: current_user.id, degree_id: 1) %>
          <%end%>
        <%end%> 
        <nav class= "navbar navbar-expand-sm navbar-light">
            <ul class= "navbar-nav">
              <li class = "nav-item active"><%= link_to 'Home', page_index_path, :class => "nav-link"%></li>
              <li class = "nav-item"><%= link_to 'Degee Audit', page_studentComplete_path, :class => "nav-link"%></li>
              <li class = "nav-item"><%=link_to "Sign Out", destroy_user_session_path, method: :delete, :class => "nav-link"%></li>
            </ul>
        </nav>
        <h2 class="text-center">Welcome</h2>

        <p>In the navigation bar at the top of the page, you will have 3 options: come back to this Home page, generate your degree audit, and sign out. Below, you can find more information about what each of these does.</p>
        <h3>Home</h3>
        <p>This will take you back to this page which provides helpful information and instructions about how to use the website.</p></br>
        <h3>Degree Audit</h3>
        <p>Here you will find a list of your completed and incompleted courses. If you see no incomplete courses, make sure to check with your administrator to ensure they have first added courses required for your major.</p></br>
        <h3>Sign Out</h3>
        <p>This will take you back to the login page</h3>
    <%end%>
  
  </div>
<%end%>
